# TASKLIST — Список задач LesSurvivalBot

> **ВАЖНО ДЛЯ AI-АГЕНТА:** 
> Перед выполнением любых задач обязательно изучи файл `README.md`. В нем описана базовая структура проекта, расположение основных функций и правила их взаимодействия. Опирайся на `README.md` для понимания архитектуры.

---

## 🚀 Текущие задачи (In Progress)
- [ ] ожидание  задачи

---

## ✅ Завершённые задачи (Done)
- [x] Исправлена логика функции `calculate_daily_ap` в `game_state.py` (расчёт AP строго по тирам HP с корректным добавлением бонусов от экипировки)
- [x] Зафиксирован «несгораемый минимум» в 1 единицу для ресурсов (`hp >= 1`, `hunger >= 1`, `thirst >= 1`) в методах `consume_action` и `process_damage`
- [x] Настроена обработка перенапряжения при получении урона в `game_math.py` без ухода параметров ниже 1
- [x] Реализован модуль `modules/hints.py` для контекстных подсказок по факелу
- [x] Настроен триггер 1-й сюжетной истории на 4-е исследование с экипированным факелом (`torch_research_count`)