# Лесная игра (LesSurvivalBot)

Telegram-бот: survival RPG в сеттинге тёмного леса, 7 локаций, нелинейный сюжет, система кармы, 7 сюжетных финалов.
Стек: Python 3.10+, aiogram 3 (Router, CallbackQuery, FSM), MongoDB / Memory fallback, aiohttp (health check & keep-alive).

---

## 🛑 STRICT & CRITICAL AI AGENT RULES (ПРАВИЛА ДЛЯ ИИ-ПОМОЩНИКА)

> [!IMPORTANT]
> Эти правила обязательны для любого ИИ-ассистента и разработчика, работающего с репозиторием. Запрещено удалять, сокращать или игнорировать этот блок. При любом изменении файла правила должны сохраняться в полном объёме.

### 1. ЧТЕНИЕ ДОКУМЕНТАЦИИ И АРХИТЕКТУРЫ
- Читай `README.md` **от начала до конца**. Конец документа — блок **«Последнее обновление»** с датой.
- Не выдумывай несуществующие файлы, вторые таблицы рецептов или дублирующие структуры состояния.
- Всегда опирайся на принцип **Fat Module, Thin Loader**: логика живёт в специализированных модулях и сервисах, `main.py` выступает только тонким диспетчером событий Telegram.

### 2. ПРАВИЛА РАЗМЕЩЕНИЯ ФАЙЛОВ И ТЕСТОВ
- **ЗАПРЕЩЕНО** создавать временные `.py` скрипты, отладочные файлы или вспомогательные утилиты в корне проекта (например, `check.py`, `debug.py`, `test.py`).
- Для временных экспериментов используй директорию `scratch/` в каталоге артефактов.
- Все автотесты размещаются **ТОЛЬКО** в директории `tests/` с именованием `test_*.py`.
- Структура пакетов строго фиксирована:
  - `services/` — инфраструктурные сервисы (база данных, FSM-диалоги, web-сервер).
  - `story/` — нарративные сценарии, тексты и обработчики локаций L1–L7.
  - `modules/` — игровые механики (предметы, готовка, ловушки, лут, подсказки).
  - `tests/` — автотесты pytest.
  - Корень — только основные оркестраторы: `main.py`, `game_state.py`, `game_math.py`, `crafts.py`, `keyboards.py`.

### 3. СТРОГИЕ ЗАПРЕТЫ ПО КОДУ
- **ЗАПРЕЩЕНО** возвращать удалённый `CAMPFIRE_RECIPES` в `main.py` — рецепты готовки живут строго в `modules/cooking.py`.
- **ЗАПРЕЩЕНО** создавать класс-дублёр `Game` в `main.py` — единственным источником правды является класс `GameState` в `game_state.py`, а `Game = GameState` экспортируется как алиас для обратной совместимости.
- **ЗАПРЕЩЕНО** изменять канонические тексты в `story/location_sources.py` без прямого указания пользователя.

### 4. ЗАПРЕТ НА ИЗМЕНЕНИЕ СЕТЕВОЙ КОНФИГУРАЦИИ И ПИНГА
- Категорически запрещено удалять, комментировать, сокращать или изменять ссылки в файле `services/server.py` (в частности список `PING_URLS`). Все указанные там адреса зафиксированы навсегда и должны оставаться в неизменном виде, независимо от их текущего HTTP-статуса.

### 5. ЗАЩИТА ПЕРСИСТЕНТНОСТИ И БАЗЫ ДАННЫХ MONGODB
- **ЗАПРЕЩЕНО** без прямого указания пользователя:
  - ломать/упрощать/удалять подключение Mongo и `save_game`/`load_game` в `services/database.py`;
  - убирать `pymongo` / `dnspython` из `requirements.txt`;
  - выкидывать или «упрощать» поля прогресса и имени из `GameState.to_document()` / `from_document()`;
  - удалять вызовы `save_game(uid, game)` после имени персонажа и после действий, меняющих мир (исследование, сон, крафт, еда, костёр, топливо и т.д.).
- При сомнениях по персистентности — **спросить** пользователя, не «улучшать молча».

---

## Архитектура проекта

```
LesSurvivalBot/
├── main.py                  — Тонкий оркестратор Telegram (aiogram 3), роутинг callback-запросов
├── game_state.py            — Единый источник правды (GameState, Game = GameState): HP, голод, жажда, AP, инвентарь, экипировка
├── game_math.py             — Математика выживания: урон, расход ресурсов по HP, пороги AP
├── crafts.py                — Глобальный крафт предметов (Факел, Костёр), проверка лимитов и экипировка
├── keyboards.py             — Генерация Inline- и Reply-клавиатур интерфейса Telegram
├── services/
│   ├── __init__.py
│   ├── database.py          — Подключение к MongoDB, in-memory fallback (MemoryPlayersCollection), load_game/save_game, кэш games
│   ├── dialogs.py           — FSM-обработчики текстового ввода (имя питомца, топливо костра, выброс предметов)
│   └── server.py            — aiohttp HTTP health-check сервер (порт 8080) и фоновый keep-alive пингер Render
├── story/
│   ├── __init__.py
│   ├── location_sources.py  — Канонические исходные тексты локаций L1–L6
│   └── location_stories.py  — Интерактивные нарративные ветки L1–L7, 7 концовок (resolve_ending, ending_text)
├── modules/
│   ├── __init__.py
│   ├── items.py             — Реестр предметов ITEMS, типы, эффекты расходников, is_item_consumable
│   ├── cooking.py           — Кулинария у костра: COOKING_RECIPES, cook_item
│   ├── traps.py             — Охотничьи ловушки: установка, утренняя проверка (40% пусто / 20% сломалась / 40% добыча), лут
│   ├── finds.py             — Лут исследования по локациям: LOCATION_FINDS, BONUS_FINDS, roll_find
│   └── hints.py             — Контекстные подсказки в статус-баре
├── tests/
│   ├── __init__.py
│   ├── test_consumables.py      — Тесты расхода воды, еды, зелий, ягод и грибов
│   ├── test_campfire.py         — Тесты розжига и сгорания костра
│   ├── test_torch_and_bottle.py — Тесты факела (1 шт, левая рука, бонус сна), бутылки воды и слота фляги
│   ├── test_game_mechanics.py   — Комплексные тесты механик (R1–R6, S1–S5, инвентарь, костёр, навигация)
│   └── test_persistence.py    — Тесты надёжного сохранения в MongoDB (to_document, BSON-типы, создание персонажа)
├── Procfile                 — Запуск воркера на Render / Heroku
├── render.yaml              — Конфигурация деплоя сервисов Render
├── requirements.txt         — Зависимости проекта
└── README.md                — Главное руководство и системный манифест
```

---

## Спецификация игровых механик

### 1. Факел (Освещение и исследование)
- **Лимит**: Персонаж может иметь **максимум 1 факел** одновременно (учитываются и инвентарь, и руки). Повторный крафт заблокирован (`can_craft` возвращает `False`).
- **Слот экипировки**: Факел помещается **строго в левую руку** (`hand_left`). В правую руку (`hand_right`) взять факел нельзя.
- **Счётчик исследований**: При исследовании с факелом в руке увеличивается `game.torch_research_count`. На 4-м исследовании гарантированно активируется главное сюжетное событие L1 (встреча с волком).
- **Шансы исследования с факелом**:
  - `40%` — ничего не найдено (осечка/темнота, факел остаётся цел).
  - `20%` — факел ломается (`hand_left` освобождается, факел уничтожается, бонус −1 AP пропадает, требуется скрафтить новый).
  - `40%` — успешная добыча (лут по таблице текущей локации).
- **Бонус AP и ночное сгорание**:
  - Пока факел экипирован в левой руке, он даёт **+1 AP** к текущему и суточному запасу действий.
  - Ночью во время сна факел сгорает и исчезает (из руки и инвентаря), никаких утренних бонусов не даёт, а его +1 AP пропадает вместе со сгоревшим факелом.

### 2. Костёр (Обогрев и стоянка)
- **Крафт**: Создаётся в `crafts.py` по рецепту: `Ветка x5`, `Камень x4`, `Мох x1`. Скрафченный костёр попадает в инвентарь как предмет.
- **Розжиг**: Выполняется через использование предмета «Костёр» в инвентаре.
  - Затраты на розжиг: **2 AP**, **7 голода**, **18 жажды** (с учётом модификаторов здоровья).
  - После розжига предмет «Костёр» списывается из инвентаря.
  - Устанавливаются параметры: `campfire_active = True`, `campfire_durability = 10`, `campfire_max_durability = 10`.
- **Интерфейс**: На главном экране появляется кнопка `🔥 Костёр 10/10`.
- **Сгорание**:
  - Каждое игровое действие с тратой `AP > 0` уменьшает прочность костра на 1 деление.
  - За ночь костёр теряет 3 деления прочности.
  - Если костёр погас (`campfire_durability <= 0`), кнопка скрывается, а утром персонаж промерзает и получает штраф **−1 AP**.

### 3. Вода, Бутылка / Фляга и Готовка
- **Стартовый запас**: Новый игрок начинает с **2 бутылками воды** (`"Бутылка воды": 2`), каждая содержит **20 глотков**.
- **Слот фляги**: В `equipment` выделен специальный слот `"flask"` («🧴 Фляга / Бутылка»).
- **Активация кнопки «Пить»**:
  - Чтобы кнопка «Пить» появилась на главном экране, нужно открыть инвентарь, выбрать «Бутылка воды» и нажать **«🧴 Надеть на пояс (в слот фляги)»**.
  - Бутылка списывается из инвентаря, надевается в слот `flask`, а счётчик зарядов устанавливается в `flask_water = 20`.
  - На главном экране отображается кнопка: `💧 Пить ({flask_water}/20)`.
- **Использование и опустошение**:
  - Нажатие `💧 Пить` восстанавливает +15 единиц жажды и отнимает 1 глоток.
  - Когда все 20 глотков выпиты: слот `flask` освобождается, в инвентарь добавляется **«Пустая бутылка»**, а в чат пишется сообщение с именем опустошённой ёмкости: `Ёмкость «Бутылка воды» опустошена!`.
- **Забор воды при готовке из нескольких ёмкостей**:
  - Если для рецепта требуется жидкость (1–3 ед.), она сначала берётся из надетой фляги/бутылки.
  - Если во фляге не хватает делений, она опустошается (давая пустую бутылку), а недостающее количество автоматически берётся из следующей бутылки в инвентаре, которая сразу надевается в слот `flask`.

### 4. Ранги качества еды и костровая кулинария
- **Цветовые маркеры рангов**:
  - ⚪ **Обычный** (ранг 1) — простейший перекус (Печёные ягоды, Жареные грибы, Сырое мясо, базовая еда).
  - 🟢 **Необычный** (ранг 2) — базовые горячие блюда (Ягодный отвар, Грибная похлёбка, Сухпай).
  - 🔵 **Редкий** (ранг 3) — сытные блюда с добычей (Мясо на коре, Зелье здоровья).
  - 🟣 **Эпический** (ранг 4) — сложное рагу (Охотничья похлёбка, Лесная тушёнка).
  - 🟡 **Легендарный** (ранг 5) — варево «Всё включено» (**Таёжный пир**: +65 сытости, +35 жажды, +50 HP).
- **Сортировка в инвентаре**: Еда автоматически отображается вверху инвентаря и отсортирована от высшего ранга (🟡) к низшему (⚪).
- **Негативные эффекты сырой пищи**:
  - Сырые ягоды: 30% шанс расстройства желудка (Жажда −4).
  - Сырые грибы: 35% шанс отравления (−6 HP, Жажда −3).
  - Светящийся гриб: 70% шанс тяжёлой интоксикации (−10 HP).
  - Сырое мясо: 50% шанс острого заражения (−12 HP, Жажда −25).
- **Карточки предметов и рецептов**: При нажатии «👁 Осмотреть», при выборе использования или рецепта костра открывается карточка с художественным описанием, эффектами, негативными рисками и примечаниями.

### 5. Охотничьи ловушки (`modules/traps.py`)
- Разблокируются на Просеке Охотников (L4) и могут устанавливаться на локациях L1–L7 (по одной на локацию).
- Проверка улова происходит утром после сна:
  - `40%` — ловушка пуста (добычи нет, ловушка остаётся взведённой).
  - `20%` — ловушка сломалась (деактивируется, добычи нет).
  - `40%` — поймана добыча согласно таблице животных локации (`TRAP_LOOT_TABLE`).

### 6. Модель экранов (Канон интерфейса и навигации)

- **ГЛАВНЫЙ (Hotbar):**
  - Верх: статус (`❤️ HP | 🍖 еда | 💧 вода | ⚡ AP | погода+день`) — строго **БЕЗ часов сервера**.
  - Верхний разделитель: `━━━━━━━━━━━━━━━━━━━`.
  - Середина: лог событий (8–12 строк) — строго **БЕЗ часов сервера**.
  - Нижний разделитель: `━━━━━━━━━━━━━━━━━━━`.
  - Кнопки:
    - Ряд 1: `🔍 Исследовать` | `🔥 Костёр {d}/{m}` (только если костёр горит)
    - Ряд 2: `🎒 Инвентарь` | `💧 Пить ({water}/20)` (если надета фляга) | `😴 Спать`
    - Дополнительно: `🌧️ Собрать воду` (при дожде), `🗺️ Локации`.

- **ИНВЕНТАРЬ:**
  - Текст: список предметов (еда — маркеры ранга 🟡/⚪/🟢/🔵/🟣; остальные предметы — собственный смайлик предмета: 🪵, 🪨, 🌿, 🧴 и т.д.).
  - Кнопки: `🔍 Осмотреть` | `🔨 Крафт` / `🗑 Выкинуть` | `↩️ Назад` -> ГЛАВНЫЙ.
  - Отдельный режим «✋ Использовать» удалён — использование перенесено прямо в карточку предмета при осмотре.

- **ОСМОТРЕТЬ (Inspect):**
  - Список предметов инвентаря: без префикса `🔍` на каждой кнопке (название с маркером/смайликом + количество).
  - **Карточка предмета:**
    - Название без `**` и без `(Армейское…)` в заголовке (`📦 {item_name}`).
    - Описание без `_курсива_`, ровные абзацы без слипания.
    - Блоки `✨ Эффекты:`, `⚠️ Негативный эффект:`, `📌 Примечание:` — аккуратные отступы без markdown-звёздочек.
    - Кнопки действий прямо в карточке для расходников (`🍽️ Съесть / Применить`, `💧 Сделать глоток`, `🧴 Надеть на пояс`, `🔦 Взять в левую руку`).
    - Кнопка `↩️ Назад` — возвращает в список осмотра (ОСМОТРЕТЬ), а не на главный экран.

- **КРАФТ:**
  - Текст: список открытых рецептов показывает сколько есть / сколько надо для каждого ингредиента: `• 🔦 Факел: Ветка (2/2), Мох (1/1)`.
  - Кнопки: иконка предмета + короткое название + статус `✅/❌ n/m` (`🔦 Факел ✅ 2/2`, `🔥 Костёр ❌ 1/3`).
  - Кнопка `↩️ Назад` — возвращает в ИНВЕНТАРЬ.
  - После успешного крафта — экран результата (`🔨 Успешно создано: ...`) с кнопкой возврата в ИНВЕНТАРЬ (`↩️ В инвентарь`).

- **ВЫКИНУТЬ:**
  - Экран выбора количества: название предмета + короткое описание + фраза «введите свое число в чат чтобы выбросить».
  - Кнопки: `🗑 Выбросить 1` | `📦 Выбросить всё` / `↩️ Назад` -> ИНВЕНТАРЬ.
  - Поддерживается прямой ввод числа в чат (FSM).

- **КОСТЁР (если горит):**
  - Текст: состояние огня живым художественным текстом (без бюрократических «делений»).
  - Кнопки: `📜 Рецепты` | `🪵 Подкинуть` / `↩️ Назад` -> ГЛАВНЫЙ.
  - Кнопка «Пожарить предмет» удалена.
  - **РЕЦЕПТЫ:**
    - Показываются ТОЛЬКО те блюда, на которые хватает ингредиентов прямо сейчас.
    - Кнопки: кружок ранга + название блюда (`⚪ Печёные ягоды`, `🟡 Таёжный пир`) без перечисления ингредиентов на кнопке.
    - Карточка рецепта: без `**`, без «легендарный ранг» в описании, без курсива. Ингредиенты в едином формате (`Любые [Ягоды]: 3`, `Вода: 3`, `Любые [Грибы]: 2`, `Кусок коры: 1`, `Сырое мясо: 1`).
    - Кнопка `↩️ Назад` — возвращает в КОСТЁР.

- **ПЕРСОНАЖ / НАСТРОЙКИ:**
  - Кнопка `↩️ Назад` — возвращает в ГЛАВНЫЙ экран.

- **ПРАВИЛО BACK (Стек экранов):**
  - Нажатие `↩️ Назад` ВСЕГДА выполняет возврат строго на 1 шаг назад в `nav_stack`:
    - Главный ← Инвентарь ← Крафт / Осмотр / Выброс
    - Осмотр ← Карточка предмета
    - Главный ← Костёр ← Рецепты / Подкинуть
    - Главный ← Персонаж / Настройки
  - Никаких зацикливаний и сбросов.

---

## Реестр файлов и ответственность

| Файл / Каталог | Что содержит | Ключевые функции и классы |
|----------------|--------------|---------------------------|
| `game_state.py` | Модель состояния персонажа, параметры, сериализация | Класс `GameState`, алиас `Game`, методы `consume_action`, `light_campfire`, `sleep_and_turn_day`, `to_document`, `from_document` |
| `game_math.py` | Формулы и математика выживания | `get_base_resource_cost`, `get_thirst_base_cost`, `calculate_ap_by_hp`, `process_damage` |
| `crafts.py` | Рецепты глобального крафта и механика экипировки | `CRAFT_RECIPES`, `can_craft`, `do_craft`, `craft_mark`, `has_torch`, `handle_craft` |
| `keyboards.py` | Разметка интерфейса aiogram | `get_main_kb`, `get_bottle_actions_kb`, `get_campfire_kb`, `inventory_inline_kb`, `character_inline_kb` |
| `main.py` | Обработка Telegram update'ов, роутинг callback | `process_callback`, `process_text_message`, `cmd_start`, `cmd_main`, `run_bot` |
| `services/database.py` | Персистентность игрового мира | `players_collection`, `load_game`, `save_game`, кэш `games`, класс `MemoryPlayersCollection` |
| `services/dialogs.py` | FSM-сценарии текстового ввода | `process_text_input`, обработчики имени питомца, дров костра и сброса предметов |
| `services/server.py` | Серверная инфраструктура | `keep_alive_pinger`, `start_health_check_server`, константа `PING_URLS` |
| `story/location_sources.py` | Исходные канонические сценарии L1–L6 | Словарь `LOCATION_SOURCE_TEXTS` |
| `story/location_stories.py` | Логика сюжетных диалогов и развилок | `handle_story`, обработчики L2–L7, `resolve_ending`, `ending_text`, `ENDING_TITLES` |
| `modules/items.py` | Реестр предметов и эффектов | Словарь `ITEMS`, кортежи `BERRY_ITEMS`, `MUSHROOM_ITEMS`, `get_item_effects`, `is_item_consumable` |
| `modules/cooking.py` | Рецепты кулинарии на стоянке | `COOKING_RECIPES`, `cook_item`, `list_recipes` |
| `modules/finds.py` | Таблицы лута и шанс находок | `LOCATION_FINDS`, `BONUS_FINDS`, `roll_find`, `apply_finds_to_inventory` |
| `modules/traps.py` | Система капканов и разделки дичи | `TRAP_LOOT_TABLE`, `place_trap`, `roll_trap_roll`, `process_trap_rollover` |
| `modules/hints.py` | Контекстные подсказки игроку | `get_active_hints` |

---

## Руководство разработчика: добавление нового контента

### 1. Как добавить новый предмет
Каждый предмет обязан быть зарегистрирован в `modules/items.py` в словаре `ITEMS`:
```python
"Название предмета": {
    "description": "Понятное игроку художественное и практическое описание.",
    "type": "food",        # допустимые типы: "food", "drink", "potion", "tool", "resource", "trap", "berry", "mushroom", "quest"
    "effects": {           # только если can_use = True
        "hunger": 20,      # восстанавливает сытость (0-100)
        "thirst": 10,      # восстанавливает жажду (0-100)
        "hp": 15,          # восстанавливает здоровье (отрицательное значение = урон)
        "poison": 5,       # опционально: периодический или прямой урон яда
    },
    "can_use": True,       # True, если предмет можно нажать/использовать из инвентаря
    "stackable": True,     # True для большинства ресурсов и расходников
}
```

### 2. Как добавить новый глобальный крафт
1. Открой `crafts.py`.
2. Добавь запись в словарь `CRAFT_RECIPES`:
```python
CRAFT_RECIPES["Новый предмет"] = [
    ("Ингредиент 1", 2),
    ("Ингредиент 2", 1),
]
```
3. Если рецепт должен быть открыт со старта, добавь его имя в `unlocked_crafts` в `GameState` (`game_state.py`).

### 3. Как добавить рецепт готовки у костра
В `modules/cooking.py` добавь запись в `COOKING_RECIPES`:
```python
"Новое блюдо": {
    "name": "Новое блюдо",
    "bark_cost": 1,             # расход коры (посуда)
    "water_cost": 2,            # расход воды из фляги (0-3)
    "ingredients": [("Сырое мясо", 1), ("[Ягоды]", 1)], # поддерживаются теги [Ягоды], [Грибы]
    "effects": {"hunger": 40, "thirst": 10, "hp": 5},
    "description": "Аппетитное горячее блюдо у костра.",
}
```

### 4. Как добавить новое поле в сохранение (Save/Load)
1. В `game_state.py` добавь поле в dataclass `GameState` с дефолтным значением.
2. В методе `to_document()` добавь сериализацию этого поля в итоговый словарь.
3. В методе `from_document()` добавь чтение и валидацию дефолтного значения для старых документов игроков.

---

## Запуск и тестирование проекта

### Запуск тестов (PowerShell / Windows)
```powershell
# Активация виртуального окружения
& ".\.venv\Scripts\Activate.ps1"

# Проверка синтаксиса ключевых файлов
py -m py_compile main.py game_state.py crafts.py

# Запуск полного набора автотестов
py -m pytest tests/ -v
```

### Локальный запуск бота
```powershell
$env:TOKEN="твой_telegram_bot_token"
py main.py
```

---

## Последнее обновление
**Дата:** 25 сентября 2026 года  
**Статус рефакторинга:** Реализована полная каноническая **Модель экранов (Hotbar, Инвентарь, Осмотр, Карточки предметов и рецептов, Крафт, Выброс, Костёр)** с пошаговой навигацией без зацикливаний и сбросов.

### Реализованные улучшения Модели экранов:
1. **ГЛАВНЫЙ (Hotbar):**
   - Верх: статус (`HP | еда | вода | AP | погода+день`) без серверных часов.
   - 8–12 строк лога событий между строгими разделителями `━━━━━━━━━━━━━━━━━━━` без серверного времени `[HH:MM]`.
   - Ряд 1: `🔍 Исследовать` | `🔥 Костёр` (если горит); ряд 2: `🎒 Инвентарь` | `💧 Пить` (если надета фляга) | `😴 Спать`.
2. **Инвентарь и карточки предметов:**
   - Список предметов различает еду (ранговые маркеры 🟡/⚪/🟢/🔵/🟣) и прочие предметы/ресурсы (собственные смайлики: 🪵, 🪨, 🌿, 🧴).
   - Кнопка «✋ Использовать» удалена из главного меню инвентаря — прямое применение/употребление перенесено в карточку предмета («🔍 Осмотреть»).
   - Карточка предмета: чистое отображение названия (`📦 {item}`), описания без курсива, блоков эффектов, негативных рисков и примечаний без markdown-звёздочек, кнопки действия и кнопка «↩️ Назад» с возвратом в список осмотра.
3. **Крафт:**
   - В описании рецептов отображается статус наличия ресурсов: `Ветка (2/2), Мох (1/1)`.
   - Кнопки крафта содержат иконку предмета, короткое название и статус: `🔦 Факел ✅ 2/2`, `🔥 Костёр ❌ 1/3`.
   - При успешном крафте отображается экран результата с кнопкой возврата в инвентарь `↩️ В инвентарь`.
4. **Выброс предметов:**
   - Экран выбора количества: название предмета, короткое описание, подсказка о вводе числа в чат и кнопки `🗑 Выбросить 1`, `📦 Выбросить всё`, `↩️ Назад`.
5. **Костёр и кулинария:**
   - Текст состояния пламени выводится в художественной форме (без бюрократических «делений»).
   - Из меню костра удалена кнопка «Пожарить предмет». Меню содержит: `📜 Рецепты`, `🪵 Подкинуть`, `↩️ Назад`.
   - Меню рецептов костра фильтрует список и показывает **ТОЛЬКО** блюда, на которые хватает ингредиентов прямо сейчас.
   - Кнопки рецептов: кружок ранга + название блюда (`⚪ Печёные ягоды`, `🟡 Таёжный пир`).
   - Карточка рецепта костра: без `**`, без курсива, ингредиенты в унифицированном формате (`Любые [Ягоды]: 3`, `Вода: 3`, `Кусок коры: 1`).
6. **Правило возврата (Стек навигации):**
   - Возврат по `↩️ Назад` всегда ведёт строго на 1 шаг назад по цепочке экранов: Главный ← Инвентарь ← Крафт / Осмотр / Выброс; Осмотр ← Карточка предмета; Главный ← Костёр ← Рецепты / Подкинуть; Главный ← Персонаж / Настройки. Зацикливания исключены.

### Пакет механик R1–R7 (Дождь, Лут, Костёр, Топливо, UI):
1. **R1 (Дождевая вода):** Кнопка «🌧️ Выпить дождевой воды». Вода не набирается во флягу/бутылку. 80% шанс напиться (+20 жажды), 20% шанс отравления/першения (-5..-8 HP с предупреждением `⚠️`). AP не тратится, экран обновляется инлайн через `get_ui()`.
2. **R2 (3 броска лута):** Бросок A по таблице локации, независимый Бросок B по таблице локации, Бросок C (~30% шанс на 1–3 палки). Повышены веса веток на локациях. Группировка одинаковых предметов в логе (`Ветка ×2`).
3. **R3 (Честный розжиг костра):** Отображение точных затрат (факел в руке — 1 AP; спички — 1 AP + 1 спичка; трение — 2 AP, 7 голода, 18 жажды). При нехватке AP отображается `⚠️ Не хватает очков действий` и блокируется розжиг.
4. **R4 (Чистая кулинария):** Готовка на костре не тратит прочность костра (`campfire_durability`) и не списывает сытость/жажду. Тратятся только ингредиенты и вода по рецепту.
5. **R5 (Топливо костра):** Кнопки `🪵 Палки (N)` (1 шт = +1 🔥) и `🧱 Кора (N)` (2 шт = +1 🔥, только чётный расход). При вводе/выборе нечётного числа коры тратится `(count // 2) * 2`, при < 2 выводится предупреждение `⚠️ Нужно чётное число коры, минимум 2`.
6. **R6 (Полировка UI):** Кнопка «✋ Осмотреть» в инвентаре (с эмодзи руки). После применения расходников лог обновляется в `get_ui()` без дублирования текста над статус-баром. Карточки блюд и костра очищены от дублирования эффектов.

### Пакет механик S1–S5 (Старт/Сейв, Имя персонажа, Кнопка Персонаж, Топливо):
1. **S1 (Канон входа и защита сохранений):**
   - **/start (Случай А — сейв есть и имя задано):** Экран существующего персонажа с атмосферным текстом и полными показателями (`👤 Выживший: {hero_name}`, `📅 День`, `❤️ Здоровье`, `🍖 Сытость`, `💧 Жажда`, `⚡ Энергия`), кнопки `[▶️ Продолжить за {hero_name}]` (`load_game`) и `[⚠️ Начать с чистого листа]` (`confirm_new_game`).
   - **/start (Случай Б — новый игрок):** Канонический `GUIDE_TEXT` с кнопкой `[🚀 Начать выживание]` (`start_new_game`).
   - **`start_new_game`:** Если сейв уже есть — автоматическое перенаправление на экран `confirm_new_game` (защита от случайного сброса). Если сейва нет — перевод в `WAITING_FOR_CHARACTER_NAME`.
   - **`confirm_new_game`:** Предупреждение: `⚠️ Внимание! Вы собираетесь начать с чистого листа. Персонаж {hero_name} ({day}-й день) и весь накопленный инвентарь будут безвозвратно удалены! Вы уверены?` и кнопки `[🔥 Да, удалить и начать заново]` (`start_new_game_confirmed`) / `[↩️ Вернуться к персонажу]` (`cancel_new_game`).
   - **`start_new_game_confirmed`:** Только здесь создаётся чистый `Game()`, запрашивается имя героя.
   - **`cancel_new_game`:** Возврат на экран Случая А со всеми показателями героя.
   - Функция `load_game` никогда молча не перезаписывает сейв пустым `Game()`.
2. **S2 (Строгий Guard ввода имени персонажа):**
   - Пока `story_state == "WAITING_FOR_CHARACTER_NAME"` или `not is_name_set`, любые игровые команды (`/main`, `/inventory`, `/character`, `/settings`) и нажатия инлайн-кнопок блокируют переход в игру и выводят вежливое напоминание: `✏️ Сначала назови своего персонажа! Напиши имя в ответном сообщении:`.
3. **S3 (Доступ к экрану персонажа):**
   - Добавлена кнопка `[👤 Персонаж]` на главном экране рядом с инвентарём (2-й ряд: `[🎒 Инвентарь] [👤 Персонаж]`).
   - Команда `/character` корректно открывает карточку снаряжения с кнопкой `[↩️ Назад]`.
4. **S4 (Меню количества топлива без лишних кнопок):**
   - В клавиатуре `get_fuel_quantity_kb` убрана кнопка `✏️ Ввести число`. Оставлены: `[+1] / [+2]`, `[Всё возможное]`, `[↩️ Назад]`.
   - Внизу сообщения выводится подсказка: `💬 Или напиши в чат число, сколько подкинуть.`.
   - Состояние `WAITING_FOR_FUEL_COUNT` выставляется автоматически сразу при выборе вида топлива (`fuel_menu:sticks` / `fuel_menu:bark`) и сбрасывается при нажатии инлайн-кнопок или возврате назад.
5. **S5 (Прозрачные и понятные расчёты топлива):**
   - Палки: `1 палка = +1 к огню`, текущий огонь `cur/max`, количество в инвентаре, сколько нужно до 10/10, сколько максимум можно подкинуть.
   - Кора: `2 коры = +1 к огню (кидаем только парами!)`, текущий огонь `cur/max`, количество коры и пар, расчёт `+{needed} огня = {needed * 2} коры`, максимум добавки с итоговым уровнем огня. Двусмысленные формулировки полностью устранены.

### Пакет персистентности MongoDB (P1–P5):
1. **P1 (Зависимости SRV-подключения):**
   - В `requirements.txt` добавлены `pymongo[srv]>=4.6.0` и `dnspython>=2.4.0`. Без `dnspython` подключение по протоколу `mongodb+srv://` (MongoDB Atlas) завершалось фатальной ошибкой `ConfigurationError`, из-за чего бот молча переключался на `MemoryPlayersCollection` (сохранение в оперативную память, сбрасывающуюся при перезапуске контейнера).
2. **P2 (Устранение AttributeError в to_document):**
   - В `game_state.py` в методе `to_document()` обращение к `display_mode`, `max_line_length`, `max_lines_per_msg` заменено на безопасный `getattr(self, ...)`. Прежнее прямое обращение вызывало `AttributeError` при каждой попытке сохранения, из-за чего `save_game()` перехватывал исключение и документ не попадал в базу.
3. **P3 (BSON-совместимость структуры данных):**
   - В `to_document()` ключи словаря ловушек `traps` принудительно преобразуются в строки (`{str(k): ...}`), так как BSON/MongoDB запрещает целочисленные ключи в словарях (`InvalidDocument: documents must have only string keys`). При десериализации в `from_document()` ключи восстанавливаются в `int`.
4. **P4 (Надёжность сетевого слоя и таймаутов):**
   - В `services/database.py` таймаут `serverSelectionTimeoutMS` увеличен с 3000 до 10000 мс для стабильного прохождения DNS SRV-резолвинга и TLS-рукопожатия на холодных стартах Render.
   - Добавлены функции `is_mongo_connected()` и `_ensure_mongo_connection()` для ленивого восстановления подключения при временных сетевых сбоях.
   - В `save_game()` и `load_game()` включён вывод полного стека ошибки (`exc_info=True`) для прозрачной диагностики в логах Render.
5. **P5 (Гарантия сохранения при создании персонажа):**
   - В `services/dialogs.py` при подтверждении имени персонажа вызов `save_game(uid, game)` защищён try/except с логированием; в случае сбоя облачной базы игроку выводится предупреждение `⚠️ Внимание: Не удалось сохранить персонажа в облачной базе данных`.
   - Если имя персонажа в документе не равно дефолтному («Выживший»), `from_document()` гарантирует флаг `game.is_name_set = True`, исключая повторный сброс в диалог ввода имени.




